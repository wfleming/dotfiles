[[ -f ~/.Xresources ]] && xrdb -merge -I$HOME ~/.Xresources

# dwm status bar
while true; do
  xsetroot -name "$(/usr/local/bin/dwmstatus)"
  sleep 30s
done &

# launch dwm, re-launch when klled (makes changing config easy)
exec ~/bin/start_dwm
