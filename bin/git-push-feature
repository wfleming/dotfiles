#!/bin/bash

TARGET=development
#TEMP=`git branch | fgrep $TARGET`
#if [ $TEMP -eq '' ]
#then
#  echo "$TARGET not a valid branch"
#  TARGET=master
#fi
FEATURE=`git branch 2>/dev/null | grep '^*' | colrm 1 2`

#echo "the target is $TARGET"

git up && git rebase $TARGET && git checkout $TARGET && git merge $FEATURE && git push