[Manager]
# Arm the SP5100 TCO hardware watchdog. If the kernel hangs or panics, PID 1
# stops petting it and the machine resets after this long. The next boot's
# "Previous system reset reason" line then says watchdog rather than a power
# cycle, which distinguishes a hang from power/firmware trouble.
RuntimeWatchdogSec=30s
